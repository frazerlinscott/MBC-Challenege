import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MBC.app',
  appName: 'MBC Challenge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
